# ========================================
# Environment Configuration
# ========================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env to version control (it's in .gitignore)

# ========================================
# Domain Configuration
# ========================================
# Your domain name (must point to your server's static IP)
DOMAIN_NAME=your-domain.com

# Email for Let's Encrypt SSL certificate notifications
# You'll receive renewal reminders and important updates
SSL_EMAIL=your-email@example.com

# ========================================
# Database Configuration
# ========================================
# Path to SQLite database file inside the container
# This is mounted to a Docker volume for persistence
DATABASE_PATH=/app/data/database.db

# ========================================
# Optional: PostgreSQL Configuration
# ========================================
# Uncomment these if migrating to PostgreSQL in the future
# POSTGRES_DB=tellme
# POSTGRES_USER=tellme_user
# POSTGRES_PASSWORD=your-secure-password
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
